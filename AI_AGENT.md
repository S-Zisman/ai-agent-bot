# AI-Agent - Диалоговый ассистент

## Новый функционал

Бот теперь оснащен **AI-диалоговым ассистентом** на базе Claude API, который:

1. Задает 7 коротких вопросов о бизнесе пользователя
2. Собирает контекст и анализирует ответы
3. Генерирует 2-3 персональных сценария внедрения AI-решений

## Архитектура решения

```
┌─────────────┐
│   Telegram  │
│     Bot     │
└──────┬──────┘
       │
       ├─── Визитка (старый функционал)
       │    ├── О Сергее
       │    ├── Программы и услуги
       │    ├── Контакты
       │    ├── Кейсы
       │    └── Консультация
       │
       └─── AI-Диалог (новый функционал)
            ├── 7 вопросов о бизнесе
            ├── Claude API (анализ ответов)
            ├── Генерация 2-3 сценариев
            └── SQLite (хранение диалогов)
```

## Технический стек

- `python-telegram-bot==22.5` - Telegram Bot API
- `anthropic>=0.40.0` - Claude API
- `python-dotenv==1.0.0` - Переменные окружения
- `sqlite3` - База данных (встроена в Python)

## Структура файлов

```
ai-agent-bot/
├── bot.py              # Основной файл бота
├── database.py         # Работа с SQLite базой данных
├── ai_questions.py     # Вопросы и интеграция с Claude API
├── requirements.txt    # Зависимости
├── .env                # API ключи (не в Git)
├── .env.example        # Пример файла с ключами
└── bot_data.db         # SQLite база (не в Git)
```

## Как это работает

### 1. Пользователь выбирает "Диалог с AI-агентом"

Бот показывает приветственное сообщение:
```
🤖 Отлично! Давай разберемся, какие AI-решения подойдут именно тебе

Я задам тебе 7 коротких вопросов о твоем бизнесе и процессах.

После этого я проанализирую твои ответы и предложу 2-3 конкретных сценария внедрения.
```

### 2. Бот задает вопросы

Вопросы (можно настроить в `ai_questions.py`):
1. О бизнесе и нише
2. Размер команды
3. Процессы, отнимающие время
4. Используемые системы (CRM и т.д.)
5. Коммуникация с клиентами
6. Повторяющиеся задачи
7. Бюджет на автоматизацию

### 3. Claude API анализирует ответы

После всех вопросов:
- Ответы отправляются в Claude API
- Claude анализирует контекст
- Генерирует 2-3 персональных сценария

### 4. Пользователь получает результат

Каждый сценарий содержит:
- Название сценария
- Что автоматизируем
- Ожидаемый эффект
- Необходимые данные/системы
- Срок внедрения

## База данных

### Таблицы

**users** - Пользователи бота
- user_id (PK)
- username
- first_name
- last_name
- created_at

**conversations** - Диалоги
- id (PK)
- user_id (FK)
- started_at
- completed_at
- status

**conversation_answers** - Ответы на вопросы
- id (PK)
- conversation_id (FK)
- question_number
- question_text
- answer
- answered_at

**scenarios** - Сгенерированные сценарии
- id (PK)
- conversation_id (FK)
- scenarios_json
- created_at

## Установка и запуск

### 1. Установите зависимости

```bash
cd ai-agent-bot
pip install -r requirements.txt
```

### 2. Настройте .env файл

```bash
cp .env.example .env
```

Заполните API ключи:
```
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
ANTHROPIC_API_KEY=your_claude_api_key
```

### 3. Запустите бота

```bash
python3 bot.py
```

## Получение API ключей

### Telegram Bot Token
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### Claude API Key
1. Зайдите на https://console.anthropic.com/
2. Перейдите в Settings → API Keys
3. Создайте новый ключ
4. Скопируйте ключ (он показывается один раз!)

## Настройка вопросов

Откройте `ai_questions.py` и отредактируйте массив `QUESTIONS`:

```python
QUESTIONS = [
    {
        "number": 1,
        "text": "Ваш вопрос здесь?",
        "type": "open"
    },
    # ... добавьте свои вопросы
]
```

## Настройка промпта для Claude

В `ai_questions.py`, метод `generate_scenarios()`:

```python
prompt = f"""Ты — эксперт по внедрению AI-решений...

{context}

Для каждого сценария укажи:
1. **Название сценария**
2. **Что автоматизируем**
3. **Ожидаемый эффект**
4. **Какие данные/системы нужны**
5. **Срок внедрения**
"""
```

## Мониторинг и аналитика

Все диалоги сохраняются в `bot_data.db`. Вы можете анализировать:

```python
from database import Database

db = Database()

# Получить все диалоги пользователя
conversations = db.get_user_conversations(user_id=123456)

# Получить ответы из диалога
answers = db.get_conversation_answers(conversation_id=1)
```

## Деплой

См. файл `DEPLOYMENT.md` для инструкций по деплою на VPS.

## Roadmap

- [ ] Интеграция с CRM (отправка лидов)
- [ ] Уведомления владельцу о новых диалогах
- [ ] Экспорт данных в Google Sheets
- [ ] Аналитика и дашборд
- [ ] Многоязычность

## Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что API ключи корректны
3. Проверьте наличие интернет-соединения
4. Откройте issue в репозитории проекта
